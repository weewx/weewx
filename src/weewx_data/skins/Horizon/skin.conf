###############################################################################
# HORIZON SKIN CONFIGURATION FILE                                             #
# Copyright (c) 2026 the WeeWX contributors                                #
# See the file LICENSE.txt for your rights.                                   #
###############################################################################

SKIN_NAME = Horizon
SKIN_VERSION = 5.5.0

###############################################################################

# Horizon draws its charts in the browser from the JSON written by the
# JSONGenerator. The plot definitions below are the familiar [ImageGenerator]
# ones -- both generators read the same section.
#
# The ImageGenerator runs as well, and writes the same PNGs Seasons always has.
# Those files keep working as stable, linkable URLs: people put them in forum
# signatures, email footers and dashboards, and none of that should break by
# moving to this skin. They also stand in for the charts when a page is opened
# without JavaScript.
#
# If you link the images nowhere, dropping the ImageGenerator from this list
# saves about half a second per report cycle and a few hundred kilobytes on
# disk. Everything on the page keeps working without it.

[Generators]
    generator_list = weewx.cheetahgenerator.CheetahGenerator, weewx.jsongenerator.JSONGenerator, weewx.imagegenerator.ImageGenerator, weewx.summaryimage.SummaryImageGenerator, weewx.reportengine.CopyGenerator

###############################################################################

[Extras]

    # If you have a Google Analytics GA4 tag, uncomment and edit the next line.
    #googleAnalyticsId = G-ABCDEFGHI


###############################################################################

[DisplayOptions]

    # Show link to NOAA-style summary reports?
    show_reports = True

    # Show a link to the RSS feed?
    show_rss = True

    # Which types appear in the RSS feed.
    observations_rss = outTemp, inTemp, barometer, windSpeed, rain, rainRate, windchill, heatindex, dewpoint, outHumidity, inHumidity

    # Which plots appear on the telemetry page.
    telemetry_plot_groups = rx, volt

    # Offer a link to the server-rendered PNG next to each chart. Those files are
    # what people put in forum signatures and email footers, so the link makes them
    # easy to find. Set to False if you drop the ImageGenerator from [Generators] --
    # the links would point at files that are no longer written.
    show_image_links = True

    # How often the page re-fetches current conditions, in seconds. Set to 0 to
    # disable live updating. There is no point setting this below your archive
    # interval -- nothing new appears until the next record is archived.
    refresh_interval = 60

    # Which types appear in "current conditions", and in what order.
    observations_current = outTemp, heatindex, windchill, dewpoint, outHumidity, barometer, windSpeed, rain, rainRate, UV, radiation, lightning_strike_count, inTemp, inHumidity, extraTemp1, extraHumid1, extraTemp2, extraHumid2, pm1_0, pm2_5, pm10_0

    # Which types are shown large, at the top of the page. Keep this short --
    # three or four is about right on a phone.
    observations_headline = outTemp, windSpeed, rain, barometer

    # Which types appear in the statistics section, and in what order.
    observations_stats = outTemp, heatindex, windchill, dewpoint, outHumidity, barometer, windSpeed, rain, rainRate, ET, hail, hailRate, snow, UV, radiation, lightning_strike_count, lightning_distance, inTemp, inHumidity, extraTemp1, extraHumid1, extraTemp2, extraHumid2, extraTemp3, extraHumid3, extraTemp4, extraHumid4, leafTemp1, leafTemp2, leafWet1, leafWet2, soilTemp1, soilTemp2, soilTemp3, soilTemp4, soilMoist1, soilMoist2, soilMoist3, soilMoist4, pm1_0, pm2_5, pm10_0, co, co2, nh3, no2, o3, so2

    # Some observations display a sum rather than min/max values
    obs_type_sum = rain, ET, hail, snow, lightning_strike_count

    # Some observations display only the max value
    obs_type_max = rainRate, hailRate, snowRate, UV

    # Sensor status fields, shown on the sensors panel.
    sensor_connections = rxCheckPercent, signal1, signal2, signal3, signal4, signal5, signal6, signal7, signal8
    sensor_batteries = outTempBatteryStatus, inTempBatteryStatus, rainBatteryStatus, hailBatteryStatus, snowBatteryStatus, windBatteryStatus, uvBatteryStatus, txBatteryStatus, batteryStatus1, batteryStatus2, batteryStatus3, batteryStatus4, batteryStatus5, batteryStatus6, batteryStatus7, batteryStatus8
    sensor_voltages = consBatteryVoltage, heatingVoltage, supplyVoltage, referenceVoltage

    # Which plots to show, and in what order. The names refer to plots defined
    # in [ImageGenerator] below, without the time span prefix: 'wind' covers
    # 'daywind', 'weekwind', 'monthwind' and 'yearwind'.
    plot_groups = tempdew, tempfeel, hum, barometer, wind, winddir, windvec, rain, radiation, UV, ET, lightning, tempin, humin, tempext, humext, templeaf, wetleaf, tempsoil, moistsoil, pm

    # The time spans offered by the period switcher. How long each one covers is
    # NOT set here -- it comes from 'time_length' in the matching [[*_images]]
    # section below, so the charts and the PNGs always show the same window.
    periods = day, week, month, year

    # Which panels appear beside the charts, and in what order. Each name is an .inc
    # file in the skin directory: to add your own, drop in 'myPanel.inc' and put
    # 'myPanel' in this list. No template editing needed.
    panels = current, sunmoon, imagery, sensors, about

    # Colours. horizon.css defines the palette; anything set here overrides it, so
    # recolouring the skin does not mean editing CSS. The names are the custom
    # properties from horizon.css -- see the top of that file for the full set.
    #
    # [[Theme]]
    #     [[[Light]]]
    #         accent = "#2c6d9e"
    #         bg = "#f4f7f9"
    #         surface = "#ffffff"
    #     [[[Dark]]]
    #         accent = "#6fa9d6"
    #         bg = "#0e161f"

    # A language switcher, shown only if there is more than one entry below.
    #
    # WeeWX renders each page in a single language, so this cannot translate the page
    # in the browser -- it links to a copy that was rendered in that language. To use
    # it, run the report once per language in weewx.conf, each with its own lang and
    # HTML_ROOT, then list them here with the path from one to the other:
    #
    #     [[HorizonReport]]
    #         skin = Horizon
    #         lang = de
    #     [[HorizonReportEN]]
    #         skin = Horizon
    #         lang = en
    #         HTML_ROOT = public_html/en
    #         [[[DisplayOptions]]]
    #             # how this rendering gets back to the top directory
    #             lang_root = ..
    #
    # The paths below are relative to the station's top directory; 'lang_root' above
    # tells a page in a subdirectory how to get back up to it.
    #
    # [[Languages]]
    #     [[[de]]]
    #         label = Deutsch
    #         path = .
    #     [[[en]]]
    #         label = English
    #         path = en


###############################################################################

[CheetahGenerator]

    # UTF-8 rather than html_entities. Every page here declares <meta charset="UTF-8">,
    # so entities buy nothing -- and they actively break the JavaScript: the encoder
    # runs over the finished document, turning an "ü" inside a script string into
    # "&#252;", which no browser decodes there. The NOAA reports below keep their own
    # encoding.
    encoding = utf8

    [[SummaryByMonth]]
        [[[NOAA_month]]]
            encoding = normalized_ascii
            template = NOAA/NOAA-%Y-%m.txt.tmpl

    [[SummaryByYear]]
        [[[NOAA_year]]]
            encoding = normalized_ascii
            template = NOAA/NOAA-%Y.txt.tmpl

    [[ToDate]]
        [[[index]]]
            template = index.html.tmpl
        [[[statistics]]]
            template = statistics.html.tmpl
        [[[celestial]]]
            template = celestial.html.tmpl
            stale_age = 3600
        [[[telemetry]]]
            template = telemetry.html.tmpl
        [[[reports]]]
            # The NOAA summaries, wrapped in a page with navigation.
            template = reports.html.tmpl
        [[[RSS]]]
            template = rss.xml.tmpl
        [[[current_json]]]
            # The small payload the page polls for live updates.
            template = current.json.tmpl


###############################################################################

# The JSONGenerator writes the time series that the browser plots.

[JSONGenerator]

    # Where the plot definitions live. Pointing at [ImageGenerator] means every
    # plot you have ever defined -- including your own additions -- is available
    # as JSON without any extra configuration.
    source = ImageGenerator

    # Subdirectory of HTML_ROOT for the JSON files.
    json_dest_dir = data

    # Decimal places. Three is plenty for weather data and keeps files small.
    round = 3

    # Emit sunrise/sunset transitions so the charts can shade the night.
    include_daynight = true

    # The four period files above are snapshots of fixed windows -- they cannot answer
    # "show me last March". The archive writes the whole record once per plot group on a
    # fixed grid, so any window over any part of the history can be shown. Because the
    # grid is regular, timestamps are implied rather than stored.
    [[Archive]]
        enable = true

        # Resolution of the archive grid. One hour over several years stays small and
        # is fine down to a day's detail; drop to 30m or 15m for a sharper zoom at the
        # cost of a proportionally larger file.
        resolution = 1h
        aggregate_type = avg

        # Which section defines the plot groups, and what to strip off the plot names
        # ('daytempdew' -> 'tempdew').
        source_group = day_images
        strip_prefix = day

        # How far back to go. 0 means the entire record.
        max_days = 0

        dest_dir = data/archive
        round = 2


###############################################################################

# A picture of the current readings, at a fixed URL. WeeWX can draw a time series
# but never the numbers -- and the numbers are what people paste into a forum post
# or a chat window. A screenshot goes stale; this file does not.

[SummaryImageGenerator]

    enable = true
    filename = current.png

    # Which readings, and in what order. Keep it to what fits: four to six is right.
    observations = outTemp, windSpeed, rain, barometer

    width = 900
    columns = 2

    # Render at this multiple, then downsample. 2 is sharp on a phone screen.
    scale = 2

    # Colours default to the skin's light palette; override to taste.
    background_color = "#ffffff"
    title_color = "#16222e"
    value_color = "#16222e"
    label_color = "#8397a7"
    sub_color = "#55666f"
    rule_color = "#dbe4ec"

###############################################################################

[CopyGenerator]

    copy_once = horizon.css, horizon.js, uplot.min.js, uplot.min.css, favicon.ico, LICENSE-uPlot.txt


###############################################################################

# The ImageGenerator section. Both generators read it: the ImageGenerator draws
# these plots as PNGs, the JSONGenerator writes the same series as JSON. The
# syntax is unchanged, so everything documented for the ImageGenerator applies.

[ImageGenerator]

    # Rendered at twice the classic 500x180. The extra pixels cost a little disk
    # but make the image sharp on the phone screens most people will open it on,
    # and it still scales down cleanly wherever it is embedded at 500 wide.
    image_width = 1000
    image_height = 360
    image_background_color = "#ffffff"   # the skin's --surface, light theme

    chart_background_color = "#ffffff"
    chart_gridline_color = "#dbe4ec"    # the skin's --border

    # Render larger still, then downsample: this is what removes the jagged
    # edges the old plots were known for.
    anti_alias = 2

    top_label_font_path = font/OpenSans-Bold.ttf
    top_label_font_size = 26

    unit_label_font_path = font/OpenSans-Bold.ttf
    unit_label_font_size = 22
    unit_label_font_color = "#5c7183"   # the skin's --ink-muted

    bottom_label_font_path = font/OpenSans-Regular.ttf
    bottom_label_font_size = 21
    bottom_label_font_color = "#8397a7" # the skin's --ink-faint
    bottom_label_offset = 6

    axis_label_font_path = font/OpenSans-Regular.ttf
    axis_label_font_size = 19
    axis_label_font_color = "#8397a7"   # the skin's --ink-faint

    rose_label = N
    rose_label_font_path = font/OpenSans-Regular.ttf
    rose_label_font_size  = 17
    rose_label_font_color = "#222222"

    # Default colors for the plot lines. The JSONGenerator passes these through
    # to the browser, so the charts inherit whatever palette you set here.
    chart_line_colors = "#4282b4", "#b44242", "#42b442", "#42b4b4", "#b442b4"
    chart_fill_colors = "#72b2c4", "#c47272", "#72c472", "#72c4c4", "#c472c4"

    line_type = 'solid'
    marker_size = 14
    marker_type ='none'
    yscale = None, None, None
    vector_rotate = 90
    line_gap_fraction = 0.05

    show_daynight = true
    daynight_day_color   = "#ffffff"
    daynight_night_color = "#eef2f6"    # close to the skin's --chart-night
    daynight_edge_color  = "#eef2f6"
    # Dusk is not a hard edge, so neither is this. Note the gradient is measured
    # in pixels, not minutes: on the day plot 40 px is roughly the half hour of
    # civil twilight, but on the week and month plots the same 40 px covers far
    # more time than dusk really takes. The browser charts do it properly, from
    # the actual twilight times.
    daynight_gradient = 40

    plot_type = line
    width = 3
    aggregate_type = none

    # Do not emit a plot if it has no non-null data:
    skip_if_empty = year

    [[day_images]]
        x_label_format = %H:%M
        bottom_label_format = %x %X
        time_length = 27h

        [[[daybarometer]]]
            [[[[barometer]]]]

        [[[daytempdew]]]
            [[[[outTemp]]]]
            [[[[dewpoint]]]]

        [[[daytempfeel]]]
            [[[[windchill]]]]
            [[[[heatindex]]]]
            [[[[appTemp]]]]

        [[[dayhum]]]
            [[[[outHumidity]]]]

        [[[daytempin]]]
            [[[[inTemp]]]]

        [[[dayhumin]]]
            [[[[inHumidity]]]]

        [[[daytempext]]]
            [[[[extraTemp1]]]]
            [[[[extraTemp2]]]]
            [[[[extraTemp3]]]]
            [[[[extraTemp4]]]]

        [[[dayhumext]]]
            [[[[extraHumid1]]]]
            [[[[extraHumid2]]]]
            [[[[extraHumid3]]]]
            [[[[extraHumid4]]]]

        [[[daytempleaf]]]
            [[[[leafTemp1]]]]
            [[[[leafTemp2]]]]

        [[[daywetleaf]]]
            [[[[leafWet1]]]]
            [[[[leafWet2]]]]

        [[[daytempsoil]]]
            [[[[soilTemp1]]]]
            [[[[soilTemp2]]]]
            [[[[soilTemp3]]]]
            [[[[soilTemp4]]]]

        [[[daymoistsoil]]]
            [[[[soilMoist1]]]]
            [[[[soilMoist2]]]]
            [[[[soilMoist3]]]]
            [[[[soilMoist4]]]]

        [[[daypm]]]
            [[[[pm1_0]]]]
            [[[[pm2_5]]]]
            [[[[pm10_0]]]]

        [[[daylightning]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 3600
            [[[[lightning_strike_count]]]]
                label = Lightning (hourly total)

        [[[dayrain]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 3600
            [[[[rain]]]]
                label = Rain (hourly total)

        [[[daywind]]]
            [[[[windSpeed]]]]
            [[[[windGust]]]]

        [[[daywinddir]]]
            line_type = None
            marker_type = box
            marker_size = 2
            [[[[windDir]]]]

        [[[daywindvec]]]
            [[[[windvec]]]]
                plot_type = vector

        [[[dayradiation]]]
            [[[[radiation]]]]
            [[[[maxSolarRad]]]]

        [[[dayUV]]]
            yscale = 0, 16, 1
            [[[[UV]]]]


        [[[dayrx]]]
            yscale = 0.0, 100.0, 25.0
            [[[[rxCheckPercent]]]]

        [[[dayvolt]]]
            [[[[consBatteryVoltage]]]]
            [[[[heatingVoltage]]]]
            [[[[supplyVoltage]]]]
            [[[[referenceVoltage]]]]

        [[[dayET]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 3600
            [[[[ET]]]]
                label = Evapotranspiration (hourly total)

    [[week_images]]
        x_label_format = %d
        bottom_label_format = %x %X
        time_length = 7d
        aggregate_type = avg
        aggregate_interval = 1h

        [[[weekbarometer]]]
            [[[[barometer]]]]

        [[[weektempdew]]]
            [[[[outTemp]]]]
            [[[[dewpoint]]]]

        [[[weektempfeel]]]
            [[[[windchill]]]]
            [[[[heatindex]]]]
            [[[[appTemp]]]]

        [[[weekhum]]]
            [[[[outHumidity]]]]

        [[[weektempin]]]
            [[[[inTemp]]]]

        [[[weekhumin]]]
            [[[[inHumidity]]]]

        [[[weektempext]]]
            [[[[extraTemp1]]]]
            [[[[extraTemp2]]]]
            [[[[extraTemp3]]]]
            [[[[extraTemp4]]]]

        [[[weekhumext]]]
            [[[[extraHumid1]]]]
            [[[[extraHumid2]]]]
            [[[[extraHumid3]]]]
            [[[[extraHumid4]]]]

        [[[weektempleaf]]]
            [[[[leafTemp1]]]]
            [[[[leafTemp2]]]]

        [[[weekwetleaf]]]
            [[[[leafWet1]]]]
            [[[[leafWet2]]]]

        [[[weektempsoil]]]
            [[[[soilTemp1]]]]
            [[[[soilTemp2]]]]
            [[[[soilTemp3]]]]
            [[[[soilTemp4]]]]

        [[[weekmoistsoil]]]
            [[[[soilMoist1]]]]
            [[[[soilMoist2]]]]
            [[[[soilMoist3]]]]
            [[[[soilMoist4]]]]

        [[[weekpm]]]
            [[[[pm1_0]]]]
            [[[[pm2_5]]]]
            [[[[pm10_0]]]]

        [[[weeklightning]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 6h
            [[[[lightning_strike_count]]]]
                label = Lightning (weekly total)

        [[[weekrain]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 6h
            [[[[rain]]]]
                label = Rain (weekly total)

        [[[weekwind]]]
            [[[[windSpeed]]]]
            [[[[windGust]]]]
                aggregate_type = max

        [[[weekwinddir]]]
            yscale = 0.0, 360.0, 45.0
            line_type = None
            marker_type = box
            marker_size = 2
            [[[[wind]]]]
                aggregate_type = vecdir

        [[[weekwindvec]]]
            [[[[windvec]]]]
                plot_type = vector

        [[[weekradiation]]]
            [[[[radiation]]]]
            [[[[maxSolarRad]]]]

        [[[weekUV]]]
            yscale = 0, 16, 1
            [[[[UV]]]]


        [[[weekrx]]]
            yscale = 0.0, 100.0, 25.0
            [[[[rxCheckPercent]]]]

        [[[weekvolt]]]
            [[[[consBatteryVoltage]]]]
            [[[[heatingVoltage]]]]
            [[[[supplyVoltage]]]]
            [[[[referenceVoltage]]]]

        [[[weekET]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 6h
            [[[[ET]]]]
                label = Evapotranspiration (weekly total)

    [[month_images]]
        x_label_format = %d
        bottom_label_format = %x %X
        time_length = 30d
        aggregate_type = avg
        aggregate_interval = 3h
        show_daynight = false

        [[[monthbarometer]]]
            [[[[barometer]]]]

        [[[monthtempdew]]]
            [[[[outTemp]]]]
            [[[[dewpoint]]]]

        [[[monthtempfeel]]]
            [[[[windchill]]]]
            [[[[heatindex]]]]
            [[[[appTemp]]]]

        [[[monthhum]]]
            [[[[outHumidity]]]]

        [[[monthtempin]]]
            [[[[inTemp]]]]

        [[[monthhumin]]]
            [[[[inHumidity]]]]

        [[[monthtempext]]]
            [[[[extraTemp1]]]]
            [[[[extraTemp2]]]]
            [[[[extraTemp3]]]]
            [[[[extraTemp4]]]]

        [[[monthhumext]]]
            [[[[extraHumid1]]]]
            [[[[extraHumid2]]]]
            [[[[extraHumid3]]]]
            [[[[extraHumid4]]]]

        [[[monthtempleaf]]]
            [[[[leafTemp1]]]]
            [[[[leafTemp2]]]]

        [[[monthwetleaf]]]
            [[[[leafWet1]]]]
            [[[[leafWet2]]]]

        [[[monthtempsoil]]]
            [[[[soilTemp1]]]]
            [[[[soilTemp2]]]]
            [[[[soilTemp3]]]]
            [[[[soilTemp4]]]]

        [[[monthmoistsoil]]]
            [[[[soilMoist1]]]]
            [[[[soilMoist2]]]]
            [[[[soilMoist3]]]]
            [[[[soilMoist4]]]]

        [[[monthpm]]]
            [[[[pm1_0]]]]
            [[[[pm2_5]]]]
            [[[[pm10_0]]]]

        [[[monthlightning]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 1d
            [[[[lightning_strike_count]]]]
                label = Lightning (daily total)

        [[[monthrain]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 1d
            [[[[rain]]]]
                label = Rain (daily total)

        [[[monthwind]]]
            [[[[windSpeed]]]]
            [[[[windGust]]]]
                aggregate_type = max

        [[[monthwinddir]]]
            yscale = 0.0, 360.0, 45.0
            line_type = None
            marker_type = box
            marker_size = 2
            [[[[wind]]]]
                aggregate_type = vecdir

        [[[monthwindvec]]]
            [[[[windvec]]]]
                plot_type = vector

        [[[monthradiation]]]
            [[[[radiation]]]]
            [[[[maxSolarRad]]]]

        [[[monthUV]]]
            yscale = 0, 16, 1
            [[[[UV]]]]


        [[[monthrx]]]
            yscale = 0.0, 100.0, 25.0
            [[[[rxCheckPercent]]]]

        [[[monthvolt]]]
            [[[[consBatteryVoltage]]]]
            [[[[heatingVoltage]]]]
            [[[[supplyVoltage]]]]
            [[[[referenceVoltage]]]]

        [[[monthET]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 1d
            [[[[ET]]]]
                label = Evapotranspiration (daily total)

    [[year_images]]
        x_label_format = %m/%d
        bottom_label_format = %x %X
        time_length = 1y
        aggregate_type = avg
        aggregate_interval = 1d
        show_daynight = false

        [[[yearbarometer]]]
            [[[[barometer]]]]

        [[[yeartempdew]]]
            [[[[outTemp]]]]
            [[[[dewpoint]]]]

        [[[yeartempfeel]]]
            [[[[windchill]]]]
            [[[[heatindex]]]]
            [[[[appTemp]]]]

        [[[yearhum]]]
            [[[[outHumidity]]]]

        [[[yeartempin]]]
            [[[[inTemp]]]]

        [[[yearhumin]]]
            [[[[inHumidity]]]]

        [[[yeartempext]]]
            [[[[extraTemp1]]]]
            [[[[extraTemp2]]]]
            [[[[extraTemp3]]]]
            [[[[extraTemp4]]]]

        [[[yearhumext]]]
            [[[[extraHumid1]]]]
            [[[[extraHumid2]]]]
            [[[[extraHumid3]]]]
            [[[[extraHumid4]]]]

        [[[yeartempleaf]]]
            [[[[leafTemp1]]]]
            [[[[leafTemp2]]]]

        [[[yearwetleaf]]]
            [[[[leafWet1]]]]
            [[[[leafWet2]]]]

        [[[yeartempsoil]]]
            [[[[soilTemp1]]]]
            [[[[soilTemp2]]]]
            [[[[soilTemp3]]]]
            [[[[soilTemp4]]]]

        [[[yearmoistsoil]]]
            [[[[soilMoist1]]]]
            [[[[soilMoist2]]]]
            [[[[soilMoist3]]]]
            [[[[soilMoist4]]]]

        [[[yearpm]]]
            [[[[pm1_0]]]]
            [[[[pm2_5]]]]
            [[[[pm10_0]]]]

        [[[yearlightning]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 1d
            [[[[lightning_strike_count]]]]
                label = Lightning (daily total)

        [[[yearrain]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 1w
            [[[[rain]]]]
                label = Rain (weekly total)

        [[[yearwind]]]
            [[[[windSpeed]]]]
            [[[[windGust]]]]
                aggregate_type = max

        [[[yearwinddir]]]
            yscale = 0.0, 360.0, 45.0
            line_type = None
            marker_type = box
            marker_size = 2
            [[[[wind]]]]
                aggregate_type = vecdir

        [[[yearwindvec]]]
            [[[[windvec]]]]
                plot_type = vector

        [[[yearradiation]]]
            [[[[radiation]]]]
            [[[[maxSolarRad]]]]

        [[[yearUV]]]
            yscale = 0, 16, 1
            [[[[UV]]]]


        [[[yearrx]]]
            yscale = 0.0, 100.0, 25.0
            [[[[rxCheckPercent]]]]

        [[[yearvolt]]]
            [[[[consBatteryVoltage]]]]
            [[[[heatingVoltage]]]]
            [[[[supplyVoltage]]]]
            [[[[referenceVoltage]]]]

        [[[yearET]]]
            plot_type = bar
            aggregate_type = sum
            aggregate_interval = 1d
            [[[[ET]]]]
                label = Evapotranspiration (daily total)
